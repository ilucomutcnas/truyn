name: Cloud PoC Semantic Retrieval A/B

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/cloud-poc-semantic-retrieval-ab.yml'
      - 'benchmarks/semantic-retrieval-ab.js'

permissions:
  contents: read
  id-token: write

jobs:
  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      TRUYN_RELAY: https://relay.truyn.org
      AZURE_RG: truyn
      AZURE_OPENAI_NAME: truyn-openai-1334540181
      AZURE_OPENAI_DEPLOYMENT: truyn-gpt-4-1-mini
      GCP_PROJECT_ID: ${{ vars.GCP_PROJECT_ID || secrets.GCP_PROJECT_ID }}
      GCP_WIF_PROVIDER_VALUE: ${{ vars.GCP_WIF_PROVIDER || secrets.GCP_WIF_PROVIDER }}
      GCP_DEPLOYER_SA_VALUE: ${{ vars.GCP_DEPLOYER_SERVICE_ACCOUNT_EMAIL || secrets.GCP_DEPLOYER_SERVICE_ACCOUNT_EMAIL }}
      GCP_RUNTIME_SA_VALUE: ${{ vars.GCP_RUNTIME_SERVICE_ACCOUNT_EMAIL || secrets.GCP_RUNTIME_SERVICE_ACCOUNT_EMAIL }}
      GCP_BENCHMARK_REGION: us-east4
      GCP_AR_REPO: truyn-poc
      GCP_PROXY_SERVICE: truyn-benchmark-gemini-direct
      GOOGLE_CLOUD_LOCATION: global
      GEMINI_MODEL: gemini-2.5-flash
      SEMANTIC_LIVE_PAIRS: '8'
      SEMANTIC_PACING_MS: '30000'
      SEMANTIC_RATE_LIMIT_RETRIES: '4'
      SEMANTIC_REQUESTER_TRANSPORT: 'http'
      SEMANTIC_BENCHMARK_OUTPUT: semantic-retrieval-ab.json
      SEMANTIC_GATE_RETRIEVAL_ACCURACY_PCT: '99'
      SEMANTIC_GATE_ANSWER_ACCURACY_PCT: '99'
      SEMANTIC_GATE_INPUT_REDUCTION_PCT: '90'
      SEMANTIC_GATE_COST_REDUCTION_PCT: '90'
      SEMANTIC_GATE_PROVENANCE_PCT: '100'
      SEMANTIC_GATE_NO_BLOCK_ID_PCT: '100'
      SEMANTIC_GATE_MINIMAL_CONTEXT_PCT: '100'
      AZURE_INPUT_USD_PER_M: '0.44'
      AZURE_OUTPUT_USD_PER_M: '1.76'
      GEMINI_INPUT_USD_PER_M: '0.30'
      GEMINI_OUTPUT_USD_PER_M: '2.50'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install --no-audit --no-fund

      - name: Verify semantic-retrieval canonical relay
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 60); do
            if curl -fsS "${TRUYN_RELAY}/health" | jq -e '.ok == true and .fastPath == true and .chainPath == true and .socketPath == true and (.contexts != null) and .semanticRetrieval == "truyn-hybrid-bm25-chargram-v1"' >/dev/null; then
              echo 'TRUYN_SEMANTIC_RELAY_HEALTH=success'
              exit 0
            fi
            sleep 5
          done
          echo 'TRUYN_SEMANTIC_RELAY_HEALTH=failed'
          exit 1

      - name: Azure login with OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID || secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID || secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID || secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Prepare direct Azure credentials
        shell: bash
        run: |
          set -euo pipefail
          endpoint="$(az cognitiveservices account show -g "$AZURE_RG" -n "$AZURE_OPENAI_NAME" --query properties.endpoint -o tsv)"
          key="$(az cognitiveservices account keys list -g "$AZURE_RG" -n "$AZURE_OPENAI_NAME" --query key1 -o tsv)"
          [[ -n "$endpoint" && -n "$key" ]] || exit 1
          echo "::add-mask::$key"
          echo "AZURE_OPENAI_ENDPOINT=$endpoint" >> "$GITHUB_ENV"
          echo "AZURE_OPENAI_API_KEY=$key" >> "$GITHUB_ENV"

      - name: Authenticate GCP deployer using WIF
        uses: google-github-actions/auth@v3
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}
          workload_identity_provider: ${{ env.GCP_WIF_PROVIDER_VALUE }}
          service_account: ${{ env.GCP_DEPLOYER_SA_VALUE }}
          create_credentials_file: true
          export_environment_variables: true

      - uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: ${{ env.GCP_PROJECT_ID }}

      - name: Prepare ephemeral direct Gemini proxy
        shell: bash
        run: |
          set -euo pipefail
          region="$GCP_BENCHMARK_REGION"
          proxy_token="$(openssl rand -hex 32)"
          echo "::add-mask::$proxy_token"
          registry="${region}-docker.pkg.dev"
          image="${registry}/${GCP_PROJECT_ID}/${GCP_AR_REPO}/semantic-benchmark:${GITHUB_SHA}"
          gcloud auth configure-docker "$registry" --quiet
          docker build -f benchmarks/Dockerfile.gemini-proxy -t "$image" .
          docker push "$image"
          gcloud run deploy "$GCP_PROXY_SERVICE" \
            --project="$GCP_PROJECT_ID" \
            --region="$region" \
            --image="$image" \
            --service-account="$GCP_RUNTIME_SA_VALUE" \
            --port=8080 --min=0 --max=1 --cpu=1 --memory=512Mi \
            --set-env-vars="BENCHMARK_PROXY_TOKEN=${proxy_token}" \
            --no-invoker-iam-check --quiet
          proxy_url="$(gcloud run services describe "$GCP_PROXY_SERVICE" --project="$GCP_PROJECT_ID" --region="$region" --format='value(status.url)')"
          for attempt in $(seq 1 30); do
            if curl -fsS -H "Authorization: Bearer ${proxy_token}" "${proxy_url}/health" | jq -e '.ok == true' >/dev/null; then break; fi
            [[ "$attempt" -lt 30 ]] || exit 1
            sleep 2
          done
          echo "VERTEX_API_ENDPOINT=$proxy_url" >> "$GITHUB_ENV"
          echo "GCP_ACCESS_TOKEN=$proxy_token" >> "$GITHUB_ENV"

      - name: Run semantic retrieval A/B
        shell: bash
        run: node benchmarks/semantic-retrieval-ab.js

      - name: Print semantic gate summary
        if: ${{ success() }}
        run: jq '{benchmark,status,context,semanticGate,aggregate,methodology}' semantic-retrieval-ab.json

      - name: Upload semantic retrieval evidence
        if: ${{ always() && hashFiles('semantic-retrieval-ab.json') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: truyn-semantic-retrieval-${{ github.run_id }}
          path: semantic-retrieval-ab.json
          if-no-files-found: error
          retention-days: 30

      - name: Enforce semantic retrieval gate
        if: ${{ hashFiles('semantic-retrieval-ab.json') != '' }}
        shell: bash
        run: |
          set -euo pipefail
          jq -e '.semanticGate.passed == true' semantic-retrieval-ab.json >/dev/null
          echo 'TRUYN_SEMANTIC_RETRIEVAL_GATE=passed'

      - name: Delete ephemeral direct Gemini proxy
        if: ${{ always() }}
        shell: bash
        run: |
          set +e
          gcloud run services delete "$GCP_PROXY_SERVICE" \
            --project="$GCP_PROJECT_ID" \
            --region="$GCP_BENCHMARK_REGION" \
            --quiet >/dev/null 2>&1 || true
