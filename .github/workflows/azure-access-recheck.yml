name: Azure Access Recheck

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/azure-access-recheck.yml
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Azure login with OIDC
        uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID || secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID || secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID || secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Inspect effective access
        shell: bash
        run: |
          set -euo pipefail
          subscription_id="$(az account show --query id -o tsv)"
          principal_object_id="4845be35-9952-4436-abfa-8a9aa3eb2639"
          az account get-access-token --resource https://management.azure.com/ --query accessToken -o tsv >/dev/null

          echo "AZURE_LOGIN=ok"
          echo "ARM_TOKEN=ok"
          echo "SUBSCRIPTION_SCOPE_READ=ok"
          echo "VISIBLE_RESOURCE_GROUPS=$(az group list --query 'length(@)' -o tsv)"
          echo "VISIBLE_RESOURCES=$(az resource list --query 'length(@)' -o tsv)"

          role_count="$(az role assignment list --all --query "length([?principalId=='${principal_object_id}'])" -o tsv)"
          role_names="$(az role assignment list --all --query "[?principalId=='${principal_object_id}'].roleDefinitionName" -o tsv | sort -u | paste -sd, -)"
          echo "EFFECTIVE_ROLE_ASSIGNMENTS=${role_count}"
          echo "EFFECTIVE_ROLE_NAMES=${role_names:-none}"

          for ns in Microsoft.Resources Microsoft.App Microsoft.CognitiveServices Microsoft.ContainerRegistry Microsoft.ManagedIdentity Microsoft.Authorization Microsoft.OperationalInsights; do
            state="$(az provider show --namespace "$ns" --query registrationState -o tsv 2>/dev/null || echo inaccessible)"
            echo "PROVIDER_${ns//./_}=${state}"
          done

          az role assignment list --scope "/subscriptions/${subscription_id}" --query 'length(@)' -o tsv >/dev/null
          echo "RBAC_READ=ok"

      - name: Verify resource-group write access
        shell: bash
        run: |
          set -euo pipefail
          probe_rg="truyn-access-probe-${GITHUB_RUN_ID}"
          cleanup() { az group delete --name "$probe_rg" --yes --no-wait >/dev/null 2>&1 || true; }
          trap cleanup EXIT
          az group create --name "$probe_rg" --location eastus --tags purpose=truyn-access-probe ephemeral=true >/dev/null
          echo "RESOURCE_GROUP_CREATE=ok"
          az group delete --name "$probe_rg" --yes >/dev/null
          trap - EXIT
          echo "RESOURCE_GROUP_DELETE=ok"
