# Installers

Target OS installers for Windows, macOS and Linux plus uninstall/recovery support.

Installers eventually must implement the lifecycle documented in `docs/getting-started/NODE_LIFECYCLE.md`: verified binaries, identity/bootstrap initialization, secure key handling, storage/config initialization and background-service registration.

Until those steps are implemented and tested, installer/script placeholders MUST NOT present themselves as working production installers.
